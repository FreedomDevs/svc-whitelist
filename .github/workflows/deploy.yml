name: Build and Deploy to Registry

on:
  workflow_dispatch:

jobs:
  call-shared-workflow:
    secrets: inherit 
    uses: FreedomDevs/.github/.github/workflows/service-deploy2.yml@main
    with:
      SERVICE_NAME: "svc-whitelist"
